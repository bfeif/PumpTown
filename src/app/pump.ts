export class Pump {
    // id: number;
    lat: number;
    lon: number;
    notes: string;

    constructor(lat, lon, notes) {
        // this.id = id;
        this.lat = lat;
        this.lon = lon;
        this.notes = notes;
    }
}